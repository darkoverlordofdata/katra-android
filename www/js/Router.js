// Generated by CoffeeScript 1.6.3
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define(["jquery", "backbone", "views/HomeView", "views/ReplView", "views/AboutView"], function($, Backbone, HomeView, ReplView, AboutView) {
  var Router, _ref;
  return Router = (function(_super) {
    __extends(Router, _super);

    function Router() {
      _ref = Router.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    Router.prototype.homeView = null;

    Router.prototype.replView = null;

    Router.prototype.aboutView = null;

    Router.prototype.initialize = function() {
      return Backbone.history.start();
    };

    Router.prototype.routes = {
      "": "home",
      "repl": "repl",
      "about": "about"
    };

    Router.prototype.home = function() {
      console.log("Route [home]");
      if (this.homeView == null) {
        this.homeView = new HomeView;
      }
      return this.homeView.render();
    };

    Router.prototype.repl = function() {
      console.log("Route [repl]");
      if (this.replView == null) {
        this.replView = new ReplView;
      }
      return this.replView.render();
    };

    Router.prototype.about = function() {
      console.log("Route [about]");
      if (this.aboutView == null) {
        this.aboutView = new AboutView;
      }
      return this.aboutView.render();
    };

    return Router;

  })(Backbone.Router);
});
